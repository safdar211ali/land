<%= form_for(@contact, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @contact.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h4>

      <ul>
      <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="form-group">
      <%= f.label 'Teacher Name', class: "col-sm-2 control-label" %>
      <div class="col-sm-10 ur-font">
        <%= f.collection_select :teacher_id,@all_teachers,:id,:full_name,{:prompt=>'Select a Teacher'}, class: "form-control" %>
      </div>
    </div>
      <div class="form-group">
    <%= f.label 'Contact type', class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :contactname,:placeholder=>'Contact type', class: "form-control" %>
    </div>
  </div>
    <div class="form-group">
      <%= f.label 'رابطے کی قسم', class: "col-sm-2 control-label ur-font" %>
      <div class="col-sm-10 ur-rtl">
        <%= f.text_field :urcontactname,:placeholder=>'رابطے کی قسم', class: "form-control" %>
      </div>
    </div>
  <div class="form-group">
    <%= f.label :contact, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :contact,:placeholder=>'Contact', class: "form-control" %>
    </div>
  </div>
    <div class="form-group">
      <div class="row">
        <div class="col-sm-offset-8 col-sm-4">
          <%=link_to contacts_path, class: "btn btn-primary" do%>
              <i class="fa fa-reply"></i> Cancel
          <%end %>
          <%= f.submit class: "btn btn-primary" %>
        </div>
      </div>
    </div>
<% end %>
